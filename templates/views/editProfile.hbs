<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap"
        rel="stylesheet">
    <title>Document</title>
    <style>
        .head {
            text-align: center;
            width:30%;
            font-family:"Poppins", sans-serif;
            background-color:rgb(19, 81, 143);
            color:white;
        }
        .sub-head {
            background-color:rgb(255, 255, 255);
            color:rgb(59, 6, 59);
            font-weight:bold;
            font-family:Cambria;
        }
        div {
            font-size : 16px;
        }
    </style>
</head>
<body>
        <section class="container">

        <div class="sec-div">
            <form class="w-50 m-auto bg-white rounded-4 p-5 shadow-lg" action="/editProfile" method="post">
                <h3 class="fw-bold">Profile</h3>
                <div class="w-50 m-auto">
                    <img class="fit-img rounded-2" src="/Assets/images/im1.jpg" alt="Profile Picture">
                </div>
                <div class="mt-5">
                    <div class="d-flex justify-content-between border border-secondary mb-2">
                        <div class="border border-secondary head p-2">FirstName:</div>
                        
                    </div>
                    <div class="d-flex justify-content-between border border-secondary mb-2">
                        <div class="border border-secondary head p-2">LastName:</div>
                        <input class="text-start w-75 p-2 px-5 sub-head" value="{{user.lastname}}" type="text" name="lname"></input>
                    </div>
                    <div class="d-flex justify-content-between border border-secondary mb-2">
                        <div class="border border-secondary head p-2">Email:</div>
                        <input class="text-start w-75 p-2 px-5 sub-head" value="{{user.email}}" type="email" name="email"></input>
                    </div>
                    <div class="d-flex justify-content-between border border-secondary mb-2">
                        <div class="border border-secondary head p-2">Mobile:</div>
                        <input class="text-start w-75 p-2 px-5 sub-head" value="{{user.mobile}}" type="tel" name="mobile"></input>
                    </div>
                    <div class="d-flex justify-content-between border border-secondary mb-2">
                        <div class="border border-secondary head p-2">Age:</div>
                        <input class="text-start w-75 p-2 px-5 sub-head" value="{{user.age}}" type="number" name="age"></input>
                    </div>
                </div>
                <button class="btn btn-danger mt-4" type="submit">Edit Profile</button>
            </form>
            <div class="profile-actions">

            </div>
        </div>
    </section>
</body>
</html>